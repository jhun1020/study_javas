package javaPractice;
import java.util.Scanner;

public class Main {
    public static void main(String[] args) {
        //입력
        Scanner sc = new Scanner(System.in);
        int num = sc.nextInt();
        for(int i=0; i<num; i++){
            int num2 = sc.nextInt();
        }
        


        int min_num = 
        int max_num = 
        
        for (let j = 0; j < inputs.length; j++) {
          if (min_num >= inputs[j]) {
            min_num = inputs[j];
          }
        }
        statistics_obj["min"] = min_num;
        
        console.log(statistics_obj["min"]);
        
        for (let i = 0; i < inputs.length; i++) {
          if (max_num <= inputs[i]) {
            max_num = inputs[i];
          }
        }
        statistics_obj["max"] = max_num;
        
        console.log(statistics_obj["max"]);
        

        scanner.close(); 
    }

}

